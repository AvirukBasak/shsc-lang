#include "parser.h"

/** Calls strdup, remember to free */
char *parse_str(const char *str)
{
    return NULL;
}

/** Calls strdup, remember to free */
char *parse_interpstr(const char *str)
{
    return NULL;
}
