proc main start
    var s = "hello world"
    var l = [1, 2, 3, 4, 5]
    var m = {"a": 1, "b": 2, "0": 7}

    s[3] = "x"
    l[3] = 42
    m[0] = 100

    for i from 0 to it:len(s) do
        io:print(s[i], lf)
    end
    for i from 0 to it:len(l) do
        io:print(l[i], lf)
    end
    io:print(m["a"], lf)
    io:print(m["b"], lf)
    io:print(m["0"], lf)
end
