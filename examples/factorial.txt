module math

proc test start
    var inp = input("Enter a number: ", i64)
    var res = factorial(inp)
    print(f"result = {res}\n")
end

# factorial(num)
proc factorial start
    var n = $0
    if n <= 1 then
        return 1
    else
        return n * factorial(n - 1)
    end
end
