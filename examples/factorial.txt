module main

proc main start
    var inp = input("Enter a number: ", i64)
    var res = factorial(inp)
    print(f"result = {res}\n")
end

# factorial(num)
proc factorial start
    if $[0] <= 1 then
        return 1
    else
        return $[0] * factorial($[0] - 1)
    end
end
